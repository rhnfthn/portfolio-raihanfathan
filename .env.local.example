# Local Supabase (runs on your machine via Supabase CLI + Docker)
# After you run `supabase start`, copy the anon key from the output.

NUXT_PUBLIC_SUPABASE_URL=http://localhost:54321
NUXT_PUBLIC_SUPABASE_ANON_KEY=
